import {Marionette} from '../../vendor/vendor';

export default Marionette.View.extend({
    className: "modal-view",

    show: function() {
        // this.$el.show();
    },

    hide: function() {
        // this.$el.hide();
    }
});
